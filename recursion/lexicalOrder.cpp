#include <iostream>
using namespace std;

void returnLexicalOrder(int n, int i)
{
    // base case

    // recurive case
    // loop for getting the starting digits
}

int main()
{

    int n;
    cin >> n;
    returnLexicalOrder(n, 0);
    return 0;
}